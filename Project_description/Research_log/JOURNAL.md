# Журнал исследования

[Оглавление](README.md) · [Глава 1](01_october_2017.ipynb) · [Следующий этап](PLAN.md#chapter-2)

## 2026-09-23 - октябрьский пример

**Вопрос:** как описать суточные колебания и взаимные сдвиги трёх уровней одной колонки?

**Выполнено:** выгружены измерения W_R4_C-4 за октябрь 2017 года; построены исходные
ряды, среднее в окружающем 24-часовом окне и отклонения; подобраны суточные волны;
рассчитаны месячные и дневные сдвиги. Сохранено по 8846 исходных записей на датчик.
Месячная подгонка использует по 7662 остатка; дневная проверка - 22 полных суток.

**Результат:** месячные сдвиги D1→D2, D2→D3, D1→D3 равны примерно 4,19; 3,60; 7,79 ч.
Это характеристики суточных волн, а не измеренные времена перемещения газа.

**Разобрано в обсуждении:** смысл четырёх рисунков, среднего и остатков, суточной
модели, R², амплитуды, времени пика, IQR, Q25/Q75, перехода через полночь, полных
суток и различия дневных/месячных оценок. Пользователь подтвердил, что объяснения понятны.

**Проверки метода:** шесть синтетических тестов фазы, амплитуды, полуночи, разных
временных меток, недостаточного покрытия и дубликатов. Известная проблема прежних
ноутбуков кросс-корреляции не затрагивает этот расчёт: они здесь не используются.

## 2026-09-24 - организация воспроизводимого отчёта

**Решение:** сохранить существующий CO2Flux и его технический README; добавить вход
в исследование и отдельную папку с оглавлением, главами и журналом. Форк не нужен:
код, данные и исследовательское объяснение остаются связаны в одном репозитории.

**Выполнено:** оформлена [глава 1](01_october_2017.ipynb) с объяснениями и исполняемыми
расчётами; сохранён неизменяемый снимок октябрьской выгрузки с контрольными суммами
и эталонными числовыми таблицами. PDF и HTML получаются из notebook.
Функция исходного рисунка выделена для совместного использования программой
и notebook; импорт Oracle перенесён внутрь функции получения данных. Метод расчёта
среднего и суточной гармоники не менялся.

**Проверка воспроизводимости:** все ячейки выполнены в новом ядре без Oracle;
результаты сравнены с таблицами, сохранёнными 23.09.2026. Итоговые графики и PDF
проверены перед завершением этого этапа.

**Открытые вопросы:** физические причины сдвигов; надёжность оценок отдельных дней;
поливы/влажность; фактические глубины в сантиметрах; точная математическая постановка
анализа траекторий. Наличие вопроса не означает, что соответствующий расчёт уже сделан.

**Следующий согласованный шаг:** ноябрь 2017 года для той же колонки, сравнение с октябрём.
Публикация на GitHub и отправка материалов Габитову в рамках оформления отчёта не выполнялись.

## 2026-09-24 - исправление записи версий библиотек

**Ошибка:** первая ячейка главы 1 останавливалась с `PackageNotFoundError` при
попытке узнать версию `nbformat` в окружении `transcription_env`. Для вычислений
главы эта библиотека не требуется.

**Исправлено:** отсутствие метаданных версии теперь отмечается в справочной
таблице как «метаданные версии недоступны» и не прерывает расчёт.

**Проверка:** все 11 кодовых ячеек последовательно выполнены в новом процессе
Python 3.14.4 из `transcription_env`, где метаданные `nbformat` отсутствуют.
Все шесть числовых таблиц совпали с эталонными; подключения к Oracle не было.
Метод расчёта не изменён. PDF и HTML в этом исправлении не пересоздавались.

## 2026-09-25 - три способа чтения исследования

**Запрос:** отделить краткий маршрут для Габитова и автора, результаты конкретного
этапа и подробные объяснения/код, к которым обращаются по необходимости.

**Изменено:** README стал кратким математическим маршрутом со статусами и прямыми
переходами. Глава 1 сокращена до постановки, формул, четырёх рисунков, численных
наблюдений, существенных ограничений и следующего шага. Подробные объяснения
с примерами (R², Q25/Q75, IQR, дневные сдвиги, чтение рисунков) сохранены в
[справочнике](details/README.md), с прямыми и обратными ссылками. Запуск, SQL,
версии и контекст источников вынесены туда же. Нумерация разделов главы изменена
на 1.1-1.6; номера рисунков сохранены. Соответствие старых разделов указано
в [технической справке](details/reproduction.md).

**Код:** пять коротких ячеек вызывают [chapter01.py](chapter01.py). Этот модуль
использует прежние алгоритмы из sensorDB и работает также при отдельном запуске
в PyCharm. Снимок, эталоны и оба вычислительных модуля sensorDB не изменялись.

**Проверка:** глава выполнена в чистом ядре `transcription_env` (Python 3.14.4),
четыре рисунка сохранены в notebook, все шесть таблиц совпали с эталонами.
Проверен отдельный запуск .py из другой рабочей папки. Отсутствие метаданных
`nbformat` по-прежнему не мешает расчёту. Проверены 103 локальные ссылки и переходы
к разделам. HTML и PDF пересобраны; все четыре страницы нового PDF просмотрены.
PDF от 24.09.2026 сохранён как предыдущая версия.

Новых исследовательских расчётов за другие месяцы не выполнялось.
Следующий этап по существу остаётся прежним: ноябрь для той же колонки.

## 2026-09-25 - отображение формул на GitHub

**Ошибка:** GitHub отклонял команду `\operatorname` в формулах.
Восемь вхождений в README, главе 1 и справочнике заменены на `\mathrm`;
математическое содержание сохранено. Большая формула README оформлена блоком
`math`, чтобы Markdown также сохранял экранированные фигурные скобки.

**Проверка:** API предпросмотра Markdown GitHub принял обновлённый README и
сохранил математический блок со скобками. Это предпросмотр, без публикации правок.
Глава повторно выполнена в чистом ядре, все шесть числовых таблиц совпали
с эталонами. HTML/PDF обновлены из notebook.

## 2026-09-25 - общая запись формулы для GitHub и PyCharm

**Наблюдение:** GitHub отобразил исправленные формулы, но PyCharm показывал
ограждённый блок `math` как код; остальные формулы в README уже отображались.

**Изменено:** только оболочка большой формулы README заменена на `$$`,
поддерживаемую документацией обеих программ. Скобки записаны как
`\lbrace`/`\rbrace`, а исправленная команда `\mathrm` сохранена.
Настройки PyCharm не менялись; приоритетом остаётся GitHub.

**Проверка:** API предпросмотра GitHub распознал один математический блок и
сохранил все команды формулы (с добавлением несущественных переводов строк).
Живое отображение в PyCharm после изменения ещё не подтверждено пользователем.
Notebook, расчётный код и экспорт главы этим исправлением не затронуты.

## 2026-09-25 - одобрение стиля и локальное открытие ссылок

**Предпочтение:** пользователь одобрил краткое математическое оглавление и чуть
более подробную главу с формулами и иллюстрациями; попросил сохранить этот стиль.
Предпочтение записано в правилах проекта и в постоянной памяти помощника.

**Локальная настройка:** Markdown-файлы в KDE назначены PyCharm вместо Cursor.
Создан отдельный пользовательский запускатель, передающий локальное имя файла
через `%f`. Стандартный запускатель PyCharm с `%u` принимал файловый URL
с фрагментом как внешний URI и не открывал документ в проверенном случае.

**Проверка:** системное открытие обычного README и URL справочника с `#r-squared`
подтверждено событиями `fileOpened` в журнале работающего PyCharm.
Точный раздел при таком открытии не выбирается: фрагмент отбрасывается.
Повторный щелчок пользователем из notebook ещё не проверен. Сами ссылки,
главы и расчётные результаты не менялись. Способ отмены настройки записан
в [технических заметках](details/reproduction.md).
