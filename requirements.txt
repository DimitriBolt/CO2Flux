# Python 3.11+ is required because the project uses `tomllib`.
# Install with:
#   python3 -m pip install -r requirements.txt --break-system-packages
#
# Oracle Instant Client is NOT a pip package.
# For Oracle Thick mode, install Instant Client separately on the machine.

matplotlib
openpyxl
oracledb
pandas
Pillow
python-dotenv
